[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "signal-forge"
version = "0.1.0"
description = "A TradingView open-source strategy research lab."
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "gary1033" }]
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]

