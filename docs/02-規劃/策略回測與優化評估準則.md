---
title: 策略回測與優化評估準則
tags:
  - project/SignalForge
  - trading/backtest
  - trading/research
  - strategy/evaluation
status: active
updated: 2026-05-24
aliases:
  - Strategy Evaluation Gate
  - 回測評估準則
---

# 策略回測與優化評估準則

這份筆記是 SignalForge 進行回測、策略優化、參數調整或尋找新策略時的共同判斷基準。任何策略改動都不能只用單一漂亮指標決定 keep；必須同時回答交易 edge、風險、穩健性與可執行性。

> [!warning]
> 這份準則只用於研究與 dry-run 評估，不構成投資建議。`live` 仍維持 dry-run only，不接 broker、不讀 API key、不送真實訂單。

## 使用時機

以下情境都要先參考這份筆記，再決定是否繼續：

- 新增策略或從 TradingView / 論文 / 其他公開來源蒸餾策略。
- 修改進場條件、出場條件、濾網、持有期、冷卻期、停損停利或部位規則。
- 比較不同參數組合，例如 threshold、hold bars、volume multiplier、regime window。
- 判斷回測結果是否可以升級成 Phase 2 候選。
- 判斷某輪 autoresearch 的結果應該 keep、discard 還是只保留為 compare-only。

## 四層評估框架

### 1. 交易 edge

先確認策略是否真的有正期望，而不是只因為單一股票、單一期間或少數大贏家看起來漂亮。

| 指標 | 目標表現 | 解讀 |
|---|---:|---|
| Profit Factor | 初篩 `> 1.2`；升級候選 `>= 1.5`；`> 3.0` 要檢查是否少數大贏家或過擬合 | PF 是 gross profit / gross loss，只能說明贏虧總量比例，不能單獨代表風險低 |
| Expectancy | 扣基本成本後 `> 0` | 期望值比勝率重要；高勝率但平均虧損過大仍是壞策略 |
| Trade count | 單一設定至少 `30` 筆才初步可讀；跨股票/跨期間最好累積 `100-300+` 筆 | 交易筆數太少時，PF、勝率、Sharpe 都容易失真 |
| Win rate + payoff ratio | 兩者必須一起看 | 勝率低可以接受，但平均獲利要足以補償平均虧損；勝率高也不能掩蓋大虧損尾端 |

### 2. 風險與可存活性

PF 改善但最大回撤惡化，不代表策略變好；這通常只是把風險推到更不容易被單一指標看見的位置。

| 指標 | 目標表現 | 解讀 |
|---|---:|---|
| Max Drawdown | 必須在可承受範圍；若惡化，需要明確 tradeoff 理由 | 用來判斷策略是否會在還沒等到長期 edge 前就被回撤打斷 |
| Calmar Ratio | `> 1` 可接受；`> 3` 強；回測 `> 5` 要懷疑樣本不足或曲線擬合 | 年化報酬除以最大回撤，適合補足 PF 看不到的資金曲線風險 |
| Sortino Ratio | 優先補進報表；應看扣成本後 downside risk | 對偏態策略比 Sharpe 更貼近風險，因為只懲罰低於目標報酬的波動 |
| Drawdown attribution | 必須能定位到股票、期間、策略原因與 signal reason | 若不知道回撤來自哪裡，不應繼續疊加濾網或參數 |

### 3. 穩健性與抗過擬合

策略要在不同股票、期間、持有期與成本壓力下保留合理表現。單一最好看的回測不是證據，只是候選。

| 檢查 | 目標表現 | 解讀 |
|---|---:|---|
| 多股票 sweep | 不依賴單一標的；升級候選至少多數股票通過，理想是全部或接近全部通過 | SignalForge 目前用七檔 / TWSE14 / TWSE23 分層股票池做日線檢查；股票池越大越要同時看 concentration 與流動性 |
| Walk-forward / OOS | OOS 至少保留 IS 表現的 `50%+` | 低於 50% 多半代表參數或策略形狀只貼合過去 |
| Benchmark relative | 相對 buy-and-hold 或市場基準要有正 active return | 只賺錢但輸給被動持有，策略價值不足 |
| Information Ratio | `0.2-0.3` 已有意義；`0.5` 好；`0.75+` 很強；`1.0` 屬例外強 | 衡量 active return / tracking error，適合比較策略是否真的比 benchmark 有價值 |
| Deflated Sharpe / PBO 意識 | 測很多參數或策略時，不能只報最好的 Sharpe 或 PF | 測試次數越多，偶然找到漂亮回測的機率越高 |

### 4. 可執行性與成本

策略必須在合理交易成本、滑價與資料限制下仍有 edge。SignalForge 目前不做真實下單，但回測仍要先把成本敏感度放進評估。

| 檢查 | 目標表現 | 解讀 |
|---|---:|---|
| Cost stress | 1x、2x、3x 成本後 expectancy 仍為正；PF 不應跌破 `1.0` | 高 turnover 或短持有期策略特別容易被成本吃掉 |
| Turnover / overlap | overlap 應可解釋；若用 cooldown 降低 overlap，要同時看 MDD 是否惡化 | overlap 降低不等於風險降低 |
| Fill assumption | 必須說明 signal timing 與成交價格，例如 close confirmed、next open entry、exit close | 避免 look-ahead 或不可能成交假設 |
| Data boundary | 必須清楚資料頻率、時間窗、session、timezone 與是否含 extended-hours | 特別是 ORB / intraday 策略，不定義 session 就不能解讀結果 |

## Keep / Discard Gate

每輪策略研究只做一個聚焦改動，並按以下流程判斷：

1. 寫清楚假設：這輪要改善 entry quality、exit rule、overlap、cost sensitivity、drawdown 還是 benchmark relative return。
2. 固定比較基準：同一批股票、同一時間窗、同一成本設定、同一 hold bars 或明確比較多持有期。
3. 先看 artifact 是否可信：timestamp、reason、position delta、signals CSV、trace summary、trade log 與 summary 必須一致。
4. 再看交易 edge：PF、expectancy、trade count、win rate + payoff ratio。
5. 再看風險：MDD、Sortino、Calmar、drawdown attribution。
6. 再看穩健性：多股票、walk-forward / OOS、benchmark relative、cost stress。
7. 最後決定：
   - **Keep**：edge 改善，風險沒有不可接受惡化，且跨股票/成本/OOS 仍站得住。
   - **Discard**：只改善單一指標、只靠少數大贏家、交易數太少、成本壓力後失效或回撤惡化無法解釋。
   - **Compare-only**：結果有研究價值，但樣本不足、語意未啟用、或還缺風控/成本/OOS 驗證。

## 新策略導入 Gate

新增策略前，先建立或更新策略筆記，並回答：

- 原始來源是什麼；是否為 TradingView、論文、公開文章或自己的研究假設。
- 是否使用 future-looking、pivot、`request.security`、realtime bar、lookahead 或 repaint 風險。
- SignalForge 的 deterministic 版本如何定義：close confirmed、next open entry、固定 hold 或明確 exit。
- 需要的資料頻率、session、timezone、成本假設與 benchmark 是什麼。
- 第一版只實作最小 long-only entry signal，還是已經進入完整持倉 / 風控系統。
- 要和哪個現有 candidate 比較；不能只和空白 baseline 比。

## 目前 SignalForge 基準解讀

截至 2026-05-24，日線多股票主線應把以下候選當成比較錨點，而不是最終策略：

| Candidate | 現況解讀 |
|---|---|
| `confluence-score + hold=10` | 七檔 TWSE common window aggregate PF 達標，但 overlap 偏高 |
| `confluence-score + hold=10 + signal_cooldown_bars=10` | 目前較強風險降低 entry-edge 候選：aggregate PF 約 `2.324`、七檔通過、overlap 降到 `0`；但 target-state OOS 對照中 2024-2026 樣本外 `0/7` beat B&H、avg excess 約 `-143.90%`，所以不能視為完整持倉主候選 |
| `confluence-score + hold=80` 系列 | 可改善相對 buy-and-hold 的落後幅度，但會明顯拉高回撤或重新產生 overlap；目前只保留 compare-only，不升級 |
| `absolute-momentum` | 長期趨勢持有 compare-only 候選。正式 target-state 預設 `126/200` 在 full-window 1x 成本下平均報酬約 `225.78%`、avg excess 約 `-226.95%`，worst MDD 約 `-50.74%`；`vol-target 0.40 + dd-risk-off 25%/120` 可把 full-window 1x worst MDD 降到約 `-44.93%`、avg Sharpe 約 `0.738`。但 walk-forward / OOS 顯示 2024-2026 樣本外原始版本只有 `1/7` beat B&H、avg excess 約 `-105.25%`；疊加風控後變成 `0/7` beat B&H、avg excess 約 `-127.67%`。相對動能股票池篩選掃描 `lookback=63/126/252`、`topN=1/2/3/4/5/7` 後，最佳 active return 仍是近似不篩選的 `topN=7`，因此也不能升級 |
| `portfolio-relative-momentum-rotation` | 目前最強 portfolio-level 候選。七檔股票池原始 `monthly + 21 bars + top3` full-window IR 約 `0.858`，但 `2021-2022` excess 約 `-24.62%`。七檔 `breadth 42/min2` 改善 full IR 到約 `1.017`，但 `2021-2022` 仍負。14 檔 TWSE 股票池中，`breadth 42/min3 top3` 是最高報酬錨點，full-window return 約 `1974.85%`、excess 約 `1638.67%`、MDD 約 `-23.01%`、IR 約 `1.417`；`top4` 是風險調整折衷錨點，return 約 `1546.66%`、excess 約 `1210.48%`、MDD 約 `-18.61%`、active MDD 約 `-20.21%`、IR 約 `1.401`。新增單檔連續入選上限後，`top4 + max consecutive 5` 把 full-window IR 提到約 `1.515`、min rolling IR 提到約 `0.814`，且 MDD / active MDD 維持 `-18.61%` / `-20.21%`，但 max rolling top-3 share 仍約 `82.62%`，沒有真正解掉 rolling concentration。新增 liquidity gate 後，`top4 + max consecutive 5 + liquidity 500M/20 bars` full-window IR 約 `1.521`、3x 成本後 IR 約 `1.490`、active MDD 約 `-19.81%`，因此暫升為 execution-aware compare candidate；但 max rolling top-3 share 仍約 `82.62%`。sector/group cap 也已測：`groupcap2` full IR 約 `1.449`，但 min rolling IR 降到約 `0.610`，max rolling top-3 share 仍約 `81.68%`；`groupcap1` 明顯傷害 edge。補上 group-level attribution 後，`liq500m` full-window 最大貢獻群組為 `electronics`、絕對貢獻占比約 `33.90%`，前三群組約 `89.27%`；full-window 最大平均曝險群組為 `semiconductor`、平均權重約 `30.33%`，前三群組平均曝險約 `65.99%`。rolling 中 `roll02` 的 `shipping` 群組貢獻約 `75.64%`，但最大平均曝險是 `financial`、約 `15.64%`，代表集中度不只是單檔，也不只是長期高曝險，而是產業/群組 regime return 依賴。dominant group exclusion 進一步確認固定刪群組不是解法：`no shipping` full IR 約 `1.255` 但 min rolling excess 轉為 `-4.68%`，`no electronics` full IR 只剩約 `0.650` 且 MDD 到 `-27.31%`，`no semiconductor` min rolling excess 仍為正但 MDD / active MDD 惡化到約 `-25.31%` / `-26.64%` 且 max rolling top3 group share 到 `98.68%`。Canary9 held-out universe 未通過：full return 約 `14.92%`、excess 約 `-0.91%`、IR 約 `-0.002`、MDD 約 `-44.29%`，rolling 最差 excess 約 `-33.45%`、IR 約 `-1.645`，max rolling top3 symbol / group share 約 `89.54%` / `98.45%`。TWSE23 擴大股票池可把 max rolling top-3 share 降到約 `65.32%`（`top4/min3`）或 `56.62%`（`top5/min5`），但 min rolling excess / IR 轉弱，且 `top4/min3` MDD 惡化到約 `-36.64%`，因此只作 concentration diagnostic，不取代 TWSE14 compare candidate。TWSE16 adjusted `ranking_skip_bars=10` 可把 full IR 改到約 `1.186`、3x IR 約 `1.158`，但 min rolling IR 仍約 `-0.856`、min rolling excess 約 `-22.55%`，max rolling top3 group share 到 `100%`；因此只作 compare-only，`skip21` discard。`ranking_mode=group-residual` 已測，`skip10 + group-residual` full IR 約 `1.059`、3x IR 約 `1.031`、MDD 約 `-35.83%`，min rolling excess 約 `-22.91%` 且 max rolling top3 group share 仍 `100%`；比 `skip10 total-return` 更差，只保留為未來大股票池 compare tool。資料未還原權息、股票池仍小，容量/流動性也只做到成交金額初篩，所以不是穩定營利證明 |
| ORB 台股 intraday 系列 | 仍有 session / execution-state / one-and-done 語意邊界，尚未成為日線多股票基準 |

補充：2026-05-24 adjusted price 診斷已把 Yahoo `adjclose / close` 調整係數套回 TWSE OHLCV 並保留 TWSE volume。同一組 `top4 + breadth42/min3 + max consecutive 5 + liquidity 500M/20 bars` 的 full IR 降到約 `1.156`、MDD 惡化到約 `-27.97%`、min rolling IR 只剩約 `0.104`。目前已補上 TWSE14 adjusted batch manifest，14 檔共 21479 rows、missing adjustment 26、skipped rows 2482；raw/adjusted comparison artifact 也已正式化，對照顯示 raw 1x `IR 1.521 / MDD -18.61%` 降成 adjusted 1x `IR 1.156 / MDD -27.97%`。因此後續策略品質判斷要優先看 adjusted-ratio 版本、batch manifest 與 raw/adjusted comparison gate，未調整價結果只能作工程比較錨點。

補充：2026-05-24 universe audit 已把 TWSE23 股票池品質變成可重跑 artifact。以 `min_row_count=1200`、平均成交金額 `500M`、群組成員數至少 `2` 檢查時，23 檔中 16 檔 eligible；補齊 `1101,2327,2357,2379` adjusted CSV 後，TWSE16 adjusted batch manifest 共有 24391 rows、missing adjustment 30、skipped rows 2486。不過同一 `top4 / breadth42-min3 / maxconsec5 / liq500M` adjusted promotion gate 仍是 `compare-only`：full IR `0.863`、3x IR `0.832`、min rolling IR `-1.123`、min rolling excess `-29.26%`、max rolling top3 group share `98.26%`；小網格 36 組也全數失敗。新增 `ranking_skip_bars` 後，`skip10` 改善 full-window IR 與 MDD，但仍沒有修好 rolling IR、rolling excess 與 group concentration。新增 `ranking_mode=group-residual` 後，TWSE16 `skip10` 的 full IR 從 `1.186` 降到 `1.059`，MDD 從 `-27.80%` 惡化到 `-35.83%`，group concentration 仍破表；因此後續說「擴大股票池」時不能只拿 raw TWSE23 回測，也不能只調 skip 長度或 residual ranking；必須到 TWSE30+ 或重設 re-entry / contribution concentration gate 後，重新跑 universe audit、adjusted batch、raw/adjusted comparison 與 promotion gate。

補充：2026-05-24 TWSE35 expanded universe 已把大型股股票池擴到 35 檔，並補齊 12 檔 raw / processed OHLCV 與 adjusted batch。`TWSE35_adjusted_batch_manifest_20260524.json` 記錄 35 檔、53403 rows、missing adjustment 68、skipped rows 2524；universe audit 顯示 27 檔通過歷史長度、流動性與群組成員數。TWSE35 `skip10 + top4 + breadth42/min3 + maxconsec5 + liq500M` adjusted baseline 的 full IR `1.685`、3x IR `1.668`、min rolling IR `0.429`、min rolling excess `11.33%`，比 TWSE16 `skip10` 明顯改善；但 MDD `-37.80%`、active MDD `-29.98%`、full top3 group share `93.49%`、max rolling top3 group share `100%`，group regime / breadth validation 仍失敗，所以 promotion gate 仍是 `compare-only`。硬擋單成員群組的 `min_symbols_per_selected_group=2` 在 TWSE35 也不成立：full IR 降到 `1.431`，min rolling IR 轉為 `-1.482`、min rolling excess `-37.95%`，只能 discard as improvement。下一步應避免把 TWSE35 full-window 指標包裝成穩定營利，改測 realized group contribution concentration gate、re-entry 條件或更高品質股票池建構。

補充：2026-05-24 realized group contribution concentration gate 已正式測過。`portfolio_rotation_sweep.py` 新增預設關閉的 `--group-contribution-lookback-bars` 與 `--max-group-contribution-share`，只用已完成持倉的 group 權重報酬貢獻，在下一次 rebalance 暫時排除 dominant group。TWSE35 adjusted 掃描中，`gcontrib21/share0.90` 的 full IR `1.742`、3x IR `1.721`，MDD `-32.91%`、active MDD `-25.13%`，看起來比 baseline 的 full-window 風險調整更好；但 min rolling IR 轉為 `-0.022`、min rolling excess 轉為 `-5.93%`、max rolling top3 group share 仍約 `98.08%`，group regime / breadth gate 仍失敗。結論是：工具 keep，當前策略升級 discard / compare-only；不能用 full IR 改善掩蓋 rolling edge 失效。

補充：2026-05-24 re-entry cooldown gate 已正式測過。`portfolio_rotation_sweep.py` 新增預設關閉的 `--reentry-cooldown-rebalances`，股票退出投組後需等待 N 次 rebalance 才能重新入選，只使用已完成持倉與本次 target weights，不讀未來 window。TWSE35 adjusted 掃描 `1-6` 後，`reentry6` 的 MDD `-28.76%`、active MDD `-25.74%`、min rolling excess `18.63%`，比 TWSE35 baseline 的 MDD `-37.80%` 與 min rolling excess `11.33%` 更好；但 full IR 降到 `1.046`、min rolling IR 精確值只有 `0.499582`，max rolling top3 group share 仍到 `100%`，group regime / breadth gate 也失敗。結論是：工具 keep，`reentry6` 只保留 compare-only artifact；不能把 drawdown 改善單獨解讀成策略升級。

補充：2026-05-24 balanced-quality universe selector 已正式測過。`portfolio_rotation_universe_select.py` 讀取 universe audit JSON，從 source eligible rows 依 group 候選成員數與每組最多入選數建立 deterministic 子股票池。TWSE35 `max_symbols_per_group=4` 選出 16 檔，full IR `1.460`、3x IR `1.427`、active MDD `-21.80%`，但 min rolling IR 轉為 `-0.367`、min rolling excess `-12.11%`，max rolling top3 group share 仍約 `99.92%`；`max_symbols_per_group=3` 更差，min rolling IR `-0.691`、min rolling excess `-22.93%`。結論是：selector 工具 keep，現有 TWSE35 balanced 子股票池 discard as strategy upgrade；不能把較低 active MDD 單獨視為進步。

補充：2026-05-24 TWSE44 thin-group expansion 已正式測過。用 TWSE 官方 OpenAPI 上市公司基本資料 `https://openapi.twse.com.tw/v1/opendata/t187ap03_L` 補 `1103,1210,1227,1229,2023,2027,2609,2615,4904` 後，TWSE44 audit 顯示 `44` 檔中 `33` 檔 eligible、`44` 檔都有 adjusted CSV；`shipping` 補成 `2603,2609,2615` 三檔 eligible，`steel` 補成 `2002,2027` 兩檔 eligible，`telecom` 維持 `2412,3045` 兩檔 eligible，但 `cement` 與 `food` 仍各只有一檔通過 `500M` liquidity gate。Selector 選出 21 檔後，`single_member_dominant_windows` 降到 `0`，這代表補替代標的方向對單成員依賴有效；但 adjusted `TWSE44 selected21` 的 full IR 只有 `1.172`、3x IR `1.146`、MDD `-53.82%`、active MDD `-42.76%`、min rolling IR `-0.438`、min rolling excess `-23.81%`，promotion gate 仍是 `compare-only`。`groupcap2` 只把 MDD 改到 `-48.76%`、min rolling IR 仍 `-0.270`。結論是：TWSE44 data/audit/selector keep，TWSE44 selected21 與 groupcap2 compare-only，discard as current strategy upgrade；不能把「單成員診斷改善」單獨視為策略進步。

補充：2026-05-24 group breadth filter 已正式測過。`portfolio_rotation_sweep.py` 新增預設關閉的 `--group-breadth-filter`，在每次 rebalance 只用當下已知的同群組成員 lookback return 計算正動能比例，避免候選股票只靠單一同群組成員撐起排名。TWSE35 adjusted 掃描中，`gb21/share0.50/min1` 是最佳組合，full IR `1.867`、3x IR `1.848`、MDD `-32.85%`、active MDD `-20.18%`、min rolling IR `0.759`、min rolling excess `34.86%`，明顯改善 full-window 與 rolling edge；但 max rolling top3 group share 仍是 `100%`，promotion gate 仍因 drawdown above threshold、group concentration、group regime / breadth validation、single-member dominant group 與 narrow group momentum 失敗。結論是：group breadth gate keep as deterministic tool，`gb21/share0.50/min1` 只保留 compare-only；不能把 IR 改善解讀為穩定營利，也不能忽略它仍是群組 regime 依賴策略。

因此下一輪如果要優化 `confluence-score`，不要先堆更多 entry filter；優先順序應是：

1. 繼續補齊 summary artifact 的 benchmark-relative risk metrics；portfolio rotation 已有 Information Ratio、tracking error 與 active drawdown，下一步應擴到更多 rolling / OOS 檢查與更大股票池。
2. 對同一七檔 common window 做 1x / 2x / 3x cost stress。
3. 做 drawdown attribution，確認 worst MDD 來自哪檔股票、哪段期間、哪種 signal reason。
4. 對 Absolute Momentum，已知 worst MDD 主要來自 `2454` 的長回撤；`dd-risk-off 20%/60` 已因 worst MDD 惡化而 discard，`25%/120` 與 `vol-target 0.40 + dd-risk-off 25%/120` 暫留 compare-only。walk-forward / OOS 已補上；relative-momentum top-N 股票池已測但沒有改善 benchmark-relative edge。下一步若要做 drawdown control，優先測 re-entry 條件、再平衡門檻、weekly rebalance 或市場 regime，而不是只調 target vol、risk-off bars 或 top-N。
5. 做更多 rolling split 或替代 regime split，確認 edge 不只集中在單一市場狀態。
6. 停損、停利或更多出場規則應分開驗證，不能和 volatility target、walk-forward 或股票池過濾同輪混在一起。
7. 對 portfolio rotation，active risk 指標、rolling window 產生器、可選 market regime filter、可選 volatility target、可選 breadth filter、ranking skip、ranking mode、per-symbol / per-window attribution、concentration guard、單檔連續入選上限、group cap、TWSE23 擴大股票池診斷、liquidity gate、group-level attribution、group exposure summary、dominant group exclusion、canary universe、adjusted price 診斷、raw/adjusted comparison artifact、universe audit、universe selector、realized group contribution gate、re-entry cooldown gate、TWSE44 thin-group expansion 與 group breadth filter 都已補上；14 檔股票池的 `breadth lookback 42 / min positive 3` 是目前最佳 breadth gate，`top4 + max consecutive 5 + liquidity 500M/20 bars` 暫為 execution-aware compare candidate。下一步不要只微調 breadth 門檻、增加 top-N、單純加大股票池、硬拉 liquidity threshold、固定刪掉 dominant group、只調 skip 長度、改成 group-residual ranking、只掃 contribution gate threshold、繼續掃 re-entry cooldown 長度、只把既有 TWSE35 越篩越小、只補低流動性 food / cement、只掃 group breadth 門檻，或把 TWSE44 的 single-member 診斷改善包裝成策略升級；sector/group cap 沒有壓低 rolling top-3 concentration；group attribution / exposure / exclusion 顯示部分 window 是 regime return 主導，硬壓曝險或硬刪群組未必有效；canary universe 顯示目前候選未泛化，adjusted-ratio 顯示未調整價結果過度樂觀。TWSE16 adjusted 小網格也因 `roll02` 與 group concentration 全數失敗；`ranking_skip_bars=10` 雖改善 full-window IR，但 rolling gate 仍失敗；`ranking_mode=group-residual` 也只證明目前 TWSE16 小池不適合用群組平均報酬殘差排名；TWSE35 contribution gate 只改善 full-window 而傷害 rolling，TWSE35 re-entry cooldown 雖改善 drawdown 與 rolling excess 但仍卡在 min rolling IR 與 group gates；balanced-quality 子股票池降低 active MDD 但讓 rolling edge 轉負；TWSE44 selected21 修掉 single-member dominance 但讓 MDD、active MDD 與 rolling IR / excess 惡化；TWSE35 group breadth filter 雖改善 IR 與 rolling excess，但仍卡在 drawdown、group concentration、single-member dominant group 與 narrow group momentum。下一步應轉向可驗證的 drawdown / risk-off、shipping regime 風險、market / industry regime filter，或搜尋新的策略 family。

## 參考資料

- William F. Sharpe, The Sharpe Ratio: https://web.stanford.edu/~wfsharpe/art/sr/SR.htm
- CFA Institute, Sharpe Ratio and Information Ratio: https://rpc.cfainstitute.org/-/media/documents/code/gips/sharpe-ratio-and-the-information-ratio.pdf
- Thomas N. Rollinger and Scott T. Hoffman, Sortino: A Sharper Ratio: https://www.cmegroup.com/content/dam/cmegroup/education/files/sortino-a-sharper-ratio.pdf
- Bailey and Lopez de Prado, The Deflated Sharpe Ratio: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2460551
- Bailey, Borwein, Lopez de Prado and Zhu, The Probability of Backtest Overfitting: https://www.carmamaths.org/resources/jon/backtest2.pdf
- David Blitz, Joop Huij, Martin Martens, Residual Momentum: https://pure.eur.nl/en/publications/residual-momentum/
- Kent Daniel and Tobias J. Moskowitz, Momentum Crashes: https://www.nber.org/papers/w20439
- Tobias J. Moskowitz and Mark Grinblatt, Do Industries Explain Momentum?: https://www.andreisimonov.com/Microstr_PhD/MSU_09/MoskowitzGrinblatt99.pdf
- Moskowitz, Ooi and Pedersen, Time Series Momentum: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2089463
- Moskowitz and Grinblatt, Do Industries Explain Momentum?: https://doi.org/10.1111/0022-1082.00146
- Antonacci, Absolute Momentum: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2244633
- Jegadeesh and Titman, Returns to Buying Winners and Selling Losers: https://www.jstor.org/stable/2328882
- Goyal and Jegadeesh, Cross-Sectional and Time-Series Tests of Return Predictability: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2610288
- Keller and Keuning, Protective Asset Allocation: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=2759734
- Keller and Keuning, Breadth Momentum and Vigilant Asset Allocation: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3002624
- Keller and Keuning, Defensive Asset Allocation: https://papers.ssrn.com/sol3/papers.cfm?abstract_id=3212862
- TradingStrategy.ai, Walk-forward analysis: https://tradingstrategy.ai/glossary/walk-forward-analysis
- Quant Memo, Transaction Costs: https://www.quantmemo.com/concepts/transaction-costs
- VaultCharts, Profit Factor: https://www.vaultcharts.com/backtesting-metrics/profit-factor
